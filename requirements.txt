# Pinned to exact versions so a from-scratch rebuild is reproducible and a
# silent breaking upgrade (especially of the Google client libs) can't slip in.
# To bump: change a pin, reinstall, run the tests + a manual smoke, then commit.
requests==2.34.2
python-dotenv==1.2.2
google-api-python-client==2.198.0
google-auth-httplib2==0.4.0
google-auth-oauthlib==1.4.0
flask==3.1.3
