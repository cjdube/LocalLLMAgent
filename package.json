{
  "name": "wren",
  "private": true,
  "description": "JS test harness for the standalone browser scripts in chat/static/ — one suite per script. The project is Python; this exists only so those scripts have a test suite. Run: npm test",
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "<rootDir>/tests/**/*.test.js"
    ],
    "modulePathIgnorePatterns": [
      "<rootDir>/.claude/worktrees/"
    ]
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "jest-environment-jsdom": "^30.4.1"
  }
}
